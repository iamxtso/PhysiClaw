"""Reading packs off the disk into the grammar, and writing new ones —
the pack's files, the pack door (which hands each route to the
compiler), the page prints, the scaffold. Imports `spec` and `route`.
The map is in `physiclaw.conductor`."""
