"""Asking the model — the conductor's scoped calls, one subsystem: the
shapes (`decision`), the blocks a user turn is built from (`blocks`), a
move as action and args (`moves`), the call table (`calltable`), the
request as messages (`compose`), the reply reader (`answers`), the texts
(`prompts`) and the channel that sends them (`channel`). Imports `spec`
alone; the walk and its steps ask through here. The map is in
`physiclaw.conductor`."""
