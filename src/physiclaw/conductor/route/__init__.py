"""The route compiler — `route:` → the compiled moves, one file per
move, and the whole-route lints beside them. Pure over `spec`: it reads
no file; `load.pack` hands it parsed documents. The map is in
`physiclaw.conductor`."""
