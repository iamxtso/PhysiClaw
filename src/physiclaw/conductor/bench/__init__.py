"""The bench — off the phone, over what sessions recorded: replay a walk,
re-ask its decisions, mine the pages it saw, label its screens. Imports
`walk`, `micro` and `spec`. The map is in `physiclaw.conductor`."""
