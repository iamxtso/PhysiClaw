"""The step executors, one per route line kind — `walk/surface.py` is
the contract they implement, `table.py` the one table the walk is
handed. Imports `walk`, `micro` and `spec`. The map is in
`physiclaw.conductor`."""
