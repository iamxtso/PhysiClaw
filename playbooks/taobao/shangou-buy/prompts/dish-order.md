You are on 淘宝闪购's search results for the store "{store}": STORE
cards (name, rating, 起送, delivery time), each followed by a few of
its dishes. First open the store, then add exactly these dishes to
its cart, then stop:
{dishes}
OPEN — tap the card whose NAME matches "{store}" (the first
matching card; a branch the buyer named, when they named one). Tap
the name row, never a dish under it. The store's own page shows
点餐 / 评价 tabs up top. No card names that store: scroll once; still
none, escalate and say what the results show.
If the cart bar at the bottom already shows a total when you arrive,
an earlier attempt left dishes in this cart: a dish already there
shows its count between − and ＋ on its row. Add only what is still
missing; take one unit off an over-counted row with its −.
ADD — for each listed dish, in order:
1. Find it in the menu (the category tabs on the left switch the
   list; scroll the list at most twice per dish). The menu may spell
   a dish differently from the list (麦辣鸡腿堡 is 麦辣鸡腿汉堡): the
   first row that reads as the dish is it — never scroll on hunting
   for an exact spelling. A promo or bundle row (买一送一, 套餐, N人份,
   随心选) is NOT the dish unless the list asks for one; the plain dish
   sits under its category tab. Tap the ＋ at the row's RIGHT end ONCE,
   or its 选规格 button when it has one — never the title or the
   picture: they open the item page.
2. On the spec sheet choose the rows the dish names (规格 / 温度 /
   糖度 / 加料); leave a row at its default when the dish says
   nothing about it; set 数量 to the ×N the list says; tap 选好了
   once. The sheet closes — the dish is in the cart.
The cart bar at the bottom shows the running total: it must grow
after each add. When every listed dish is added, you are done. A dish
that is 售罄 or not on this menu: skip it, and say so in `added` as
"(not available) <dish>".
